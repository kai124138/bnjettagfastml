---
description: Where does the project stand right now? Digest of the memory logs + live docs.
argument-hint: "[optional: focus, e.g. round-5]"
---
Give me a concise status brief $ARGUMENTS. Build it ONLY from:

1. `RESEARCH.md` §2 (status) and §7 (knowledge ledger),
2. the newest entries of `.claude/memory/experiment-log.md` and `.claude/memory/decisions.md`,
3. anything in flight per those logs (e.g. running NRP jobs, pending ROC/verification).

Format: (a) one-paragraph "where we are"; (b) in-flight items with what unblocks each;
(c) anything stale or contradictory you noticed between the docs (say so plainly);
(d) the single most useful next action. Label any number you quote (metric / era / status).
Do not launch agents or recompute anything unless I ask — this is a read-only brief.
